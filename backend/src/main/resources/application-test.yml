spring:
  datasource:
    url: jdbc:h2:mem:gameswap_db;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialec
    hibernate:
      ddl-auto: update
  flyway:
    enabled: true